# Python
__pycache__/
*.pyc

# 运行生成物（每次运行重建，不入库）
logs/
*.dlx

# 运行记忆（含本机 DXF 路径，每次运行漂移；新机器上脚本自动重建）
nx_extrude_params.json
*.bad-*

# AI 工作目录（计划/记忆，不入库；用完可手动删除）
.zcode/
.workbuddy/

# 归零工具临时产物
stdparts_归零/
_staging_*.prt
_preview_*.prt

# 旧版实机回传的探针/报告日志目录（按机器命名，不入库）
NX10/
NX12/

# NX 向下兼容工具：x_t 交换件/导入输出/运行日志均可再生，只入脚本
tools/NX向下兼容工具/xt/
tools/NX向下兼容工具/x_t转prt/
tools/NX向下兼容工具/*_log.txt
