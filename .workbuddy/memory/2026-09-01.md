# 2026-09-01 工作日志

## NX10 实机首测结果判读 + 探针升级 v1.1

- 用户提供 NX10 实机探针结果（NX10logs/nx_compat_probe.json，Python 3.3.2 实证）
- 实测定案: PY 层与预判一致（仅 importlib.util / ast.AsyncFunctionDef 两断点，imp.load_source 可用）；UF.AskVersion 无 Python 包装 → 版本探测定案走 UGII_ROOT_DIR 路径解析（实测解析出 "NX 10.0"）；CreateRuleBaseCurveDumb/CreateSection/Promotion/RemoveParameters/DisplayModification/LayerCategories(3参) 在 NX10 实测可用
- 新发现: ①NX10 无 Matrices/PointMatrices 集合（老版应为 NXMatrices，主脚本 L2089 双候选需扩三候选；向量版 CreateArc 回退可用不阻断）②ExtrudeBuilder 全链在某行 TypeError 未定位（嫌疑: AddToSection 7参/CreateDirection 3参/Feature.Null）③UI CreateDialog 报"只能对工作部件执行"系探针环境问题非 dlx 被拒
- 探针 v1.0→v1.1: 拉伸链改分步探测（S1~S7，含 Feature.Null→None、AddToSection 7→6参、CreateDirection 3→2参回退）；矩阵三候选 NXMatrices/Matrices/PointMatrices；UI 组前置工作部件兜底；修 API004/005 重复记录 bug；API017 SKIP 提示带 stdparts
- 报告 v1.0→v1.1: 新增 §6 NX10 实机首测结果 + 附录 A 状态更新（6 项待 v1.1 复测：dlx 加载、EdgeBlend 逐属性、AddChainset、MassProperties 单位数、枚举通道、拉伸断行定位）
- 下一步: 用户将 v1.1 探针（连同 stdparts）在 NX10 复跑 + NX12 首跑，重点看 API006 断在哪步、UI002/003、API010、API013

## NX10+NX12 双机 v1.1 复测判读 + 探针 v1.2

- NX12 机器首跑（Python 3.6.1 实证，与查证的 NX12 内置版本一致）
- **重大定案: dlx 风险解除** —— NX10 与 NX12 都能加载 NX="2312.0.0" 版本戳 dlx 和省略 NX 属性变体 → 三处硬编码版本戳零改动，原 §3.3 dlx 策略与降级阶梯 L1 基本作废
- **回调名两版与 2312 一致**（AddDialogShownHandler 存在）；DialogSizing 两版均不存在（守卫降级即可）；矩阵集合两版均仅 NXMatrices 且无 CreateMatrix（向量版 CreateArc 回退已可用）
- **P1 核心代码改造点定位: S3 AddToSection** —— S1(Feature.Null)/S2(挂Section) 通过，S3 7参/6参均 TypeError；查官方文档（NX10 Java / NX1926/2212 .NET）签名与 2312 完全一致 → 判定为绑定层编组问题（list→数组 / None seed / 枚举包装三嫌疑）；NX12 报错回显实参类型 (list, NoneType×3, Point3d, SectionModeMemberType) 佐证
- **P2 交付侧问题: stdparts 为 NX2312 格式** —— NX10"不是部件文件"、NX12"部件文件出自比当前 NX 版本更新的版本"；动作: NX2312 文件→选项→保存选项→部件文件版本设兼容旧版后重新另存 14 件
- **版本探测再定案: UGII_ROOT_DIR 不可靠**（NX12 机器残留 NX10 旧值）→ v1.2 加 VER004(UGII_BASE_DIR)/VER005(NXOpen.__file__ 路径解析，首选通道)
- 探针 v1.1→v1.2: S3 升级签名梯（seed=None→首曲线 × list→.NET System.Array 共 6 梯，失败汇总每梯异常）；API017 ERROR 附加部件版本判读提示
- 待 v1.2 复测: S3 可用梯、EdgeBlend 14 属性+AddChainset、MassProperties 单位数、布尔 5/7 参（全部在 S3 解锁后同轮出）；UI004 枚举通道免测（主脚本运行时 4 通道自适应）
- 报告 v1.1→v1.2: §6 重写为双机对比定案表 + 附录 A 12 项状态（5 项已定案、6 项待 v1.2、1 项免测）
