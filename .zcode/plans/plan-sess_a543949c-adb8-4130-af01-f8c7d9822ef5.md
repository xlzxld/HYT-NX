# NX 项目代码审计与修复计划

## 一、背景与已完成工作

项目为 NX 2312 二次开发 DXF→3D 自动化脚本：`nx_extrude_runner.py`（5131 行，v1.34）+ `nx_std_config.py`（299 行）+ `nx_extrude_params.json`（运行记忆）。已通过 3 轮全文件通读完成：架构/调用链/数据流梳理、约 60 项问题排查（高 1 / 中 16 / 低约 40），关键发现已逐条人工复核行号确认。

**你已确认的决策**：①修高+中危+顺手低危一行项；②删休眠段（build_dlx 标准件组、SP* 预填循环）、保留实验函数 `_flush_start_r` 与 `_promote_body` 死分支；③CONFIG_SCHEMA_VERSION 3→4 全重置记忆；④验证=离线全验证 + dev\.zcode\ 套件（你将放入，实施时确认是否就位）+ NX 实测清单。

**实施前置**：按项目约定不自动备份——开始改动前请手动备份 `nx_extrude_runner.py`、`nx_std_config.py`、`nx_extrude_params.json`。

## 二、问题清单（本次修复项，按严重度）

### 高危（1 项）
| # | 定位 | 问题 |
|---|---|---|
| H1 | runner:767-823 | DXF 不支持实体（LWPOLYLINE/SPLINE 等）被**完全静默丢弃**：`entities_of` 只收 LINE/CIRCLE/ARC，其余类型不进任何统计；`stats["total"]` 只数受支持实体导致日志口径虚高；815-817 的 else 分支为死代码。图纸用多段线画轮廓时整层几何丢失且无任何提示 |

### 中危（16 项）
| # | 定位 | 问题 |
|---|---|---|
| M1 | runner:2563-2572 | SUBTRACT 件在 `_pick_target` 返回 None（无 FLB 体）时仍删掉刚放置提升的独立体——`if bm=="SUBTRACT"` 删体分支未嵌在布尔成功路径内，日志却称"已放置"，该锚点什么都不剩 |
| M2 | runner:2198-2208, 2519-2521 | `dir="-Z"`（绕 X 180°）时放置公式 `pos=锚点−ref+off` 未对 ref 做旋转，实际对位误差 = (0, 2·ref_y, 2·ref_z)（当前 config 全为 +Z 故未爆发） |
| M3 | runner:4622-4625 | selftest 直接解引用 `_USER_CFG.ZMODE_DEFS` 无 None 守卫 → config 缺失时自测 AttributeError 崩溃，违背自身"selftest 不依赖 config"声明 |
| M4 | runner:330-349 | `_load_user_config` 吞掉一切异常返回 None，**零日志**；config 写错一处 → 整个配置静默失效回兜底表 → 所有标准件因"未配 ref"被跳过，提示语误导排查方向 |
| M5 | runner:3950-3983 | `ParamDialog.update_cb` 把 FLB 联动+JRT 重置整段包在 try/except **pass** 里，联动失效无任何痕迹（历史事故同型） |
| M6 | runner:395-398, 433-444, 475-479, 529 | config 顶层标量非法（如 JRT_DRAFT 写成 "abc"）时 import 期 `float()/int()` 裸崩，违背 config 注释"非法值一律回落安全默认、不会报错不会崩"承诺 |
| M7 | runner:484-489 | config 缺 `ZMODE_DEFS` 键（探针只验 3 键验不到它）→ import 期 AttributeError，整个脚本不可用 |
| M8 | runner:1650-1659, 5000-5007, 1644 | JSON 坏类型崩溃链：params 为非 dict（列表/字符串）→ `.items()` AttributeError；selected 为数字 → TypeError；jrt_se 为 dict → 写入键列表脏值 |
| M9 | runner:1627-1647 | 记忆安全：load_state 吞错回 {} → 用户参数静默回默认且**下次 save 用默认值覆盖、记忆永久丢失**；save_state 非原子写（先截断再写）+ 只捕 IOError |
| M10 | runner:1650-1659, 508-525 | `merge_params` 不受 schema 门控 → 文档承诺"调大 schema 连界面参数一起回默认"未兑现（配合本次 schema 3→4 修复，使全重置行为与文档一致） |
| M11 | runner:3608-3611 | `_find` 把 FindBlock 返回的 None 也写缓存 → 此后同名块永远 None，参数收集静默回退、jrt_reset 联动到 (0,-7.5) 而非当前 FLB 值 |
| M12 | runner:830-877 | `_pkey` 网格量化无邻桶搜索：断口 <0.01mm 但跨量化格边界 → 该闭合的链判开；端点三叉及以上（T 形/重复线）按字典序任意配对 → 链错串 |
| M13 | runner:921-978 | 嵌套判定只用首顶点射线 + 无重复环处理：部分重叠/相切环父子误判；完全重合的重复描线轮廓被判为孔 → 板被掏空 |
| M14 | runner:2226-2240 | `collect_circle_anchors` 同心去重 O(n²) 且 200 上限护栏在收集完成后才检查——规则配错上万圆时先卡死 |
| M15 | runner:1508-1511, 3777-3778 | CXK 件在窗口③把定位图层切到圆心层后，dlx 静态无半径框 → 收集端静默沿用旧 r_max（可能全半径海量匹配） |
| M16 | runner:1593, 1608 | dlx 写盘失败回退 %TEMP% 用**固定文件名** → 复活 v1.14/v1.17 已根治的"NX 按 dlx 文件名回灌旧值"事故 |

### 顺手低危（一行/小改，约 15 项）
B4 open→with(733)；B5 `_set_display` 无 finally Dispose(3155-3164)；B6 `_remove_parameters` 无 finally Destroy(2424-2431)；B7 `_set_enum_idx`/DialogSizing 的 PropertyList 不 Dispose(3672-3677,3724)；B8 selftest `_ZMODE_DEFS` append/pop 无 try/finally(4629-4632)；B12 JRT"跳过删面"日志文案与行为不符(3256)；B13 超图层上限兜底落到用户第 1 层(417-419,1931)；B15 run_pipeline 异常日志无 traceback(3472-3478)；C-03 提示文案硬编码图层号(1410,1881)；B-04 窗口②Apply 后 Cancel 仍继续执行(4010-4015,5078)；A5 2502-2504 永不可达重复检查；A2 4773 孤儿注释；A4 2466 未用 uf 变量；A22 4710 冗余 import；A23 4526/4626 重复断言；A19 4925 死表达式；A3 602 过时 docstring。

### 仅记录不动代码（报告说明）
`_flush_start_r`、`_promote_body` 死分支（按你定案保留）；changelog docstring 的 20 处空版本头与 v1.13 重复段（历史留痕，不清理）；merge_jrt/build_layer 未用形参（避免无谓签名 churn）；D-03 逐锚点 DoUpdate、D-01 organize_loops O(m²·V)（正确性优先不动，报告给性能建议）；selftest 断言数随 config 条目漂移（"143 项"口径过期）。

## 三、修复批次（每批结束跑 py_compile + --selftest）

**批次 1｜数据与配置健壮性**：M4（失败原因记入模块级缓冲，Log 建立后输出+弹窗提示）、M6（新增 `_cfg_float/_cfg_int` 带回退辅助）、M7（ZMODE_DEFS getattr 守卫+探针补验）、M8（merge_params/selected/jrt_se 容错清洗）、M9（load_state 坏文件改名 `.bad-<时间戳>` 留证防覆盖；save_state 临时文件+os.replace 原子写、失败记日志）、M10（params 纳入 schema 门控）、schema 3→4（config:66）。

**批次 2｜流水线正确性**：H1（entities_of 统计所有实体类型；unsupported 汇总进日志显著警告+弹窗；修死分支）；M1（target None 时跳过删体、按独立体保留并改日志）；M2（放置公式改 `pos=锚点−R·ref+off`，抽纯函数便于自测）；M11（_find 对 None 不缓存）；M15（_collect_std 检测 CXK→圆心层切换且半径未收窄时日志警告+按 sanitize 收窄）；M16（TEMP 回退路径也走 `_fresh_dlx_path` 唯一名）。

**批次 3｜几何逻辑加固（行为变更点，全部带新增回归断言）**：M12（find_chains 邻桶 3×3 匹配替代单格；T 形多候选时按方向延续性选段）；M13（organize_loops 重复环去重：面积+bbox 全等判重复跳过；point_in_poly 多顶点投票降低首顶点偶然性）；M14（同心去重改量化 dict O(n)，护栏提前对图层实体数预估提示）。保守实现：只影响此前判错的场景，已有 4 层嵌套/开链四形态等断言保持全绿作为回归底线。

**批次 4｜对话框与低危清扫**：M5（update_cb except 写入 logs/nx_dialog_debug.txt 调试脚印而非静默）；B-04（cancel_cb 清 result 槽）；其余低危项逐条一行修。

**批次 5｜休眠段删除（同步 selftest）**：build_dlx 标准件组 1428-1457、SP* 预填循环 3897-3922 删除，`build_dlx/write_dlx` 签名同步，相关 selftest 断言（4834 段）改写为"传 std_rules 不再生成组"或删除；2502-2504、4773 孤儿注释删除。`SCRIPT_VERSION` 1.34→1.35，docstring 追加一条 v1.35 changelog（单条简洁，不重放追加）。

## 四、验证方案

1. **静态**：`python -m py_compile` 两文件。
2. **离线自测**：`python nx_extrude_runner.py --selftest` 全绿（现有 ~146 断言 + 新增约 15 条：LWPOLYLINE 统计、格点边界断口、T 形三叉、重复环去重、-Z 放置偏移、merge_params/selected 坏类型、schema 门控 params、config 标量回退辅助、load_state 坏文件备份/原子写（临时目录）、_find None 不缓存、锚点去重压力 2000 圆限时、大图 500 环冒烟）。
3. **dev\.zcode\ 套件**：实施时确认目录是否就位；就位则按其说明跑批量回归（3Dtest 全家族两遍、01.dxf 加热条）；未就位则明确告知并转入 NX 实测清单。
4. **NX 实测清单（交给你执行）**：①窗口①②③全流程+重跑清理重建一致；②schema 4 首次运行应提示记忆重置、config 实测 ref 生效（14 件不再被跳过）；③含 LWPOLYLINE 的图纸应出现"不支持实体"警告且几何不丢；④SUBTRACT 件在无 FLB 场景保留独立体；⑤故意改错 config 一处 → 有明确日志/弹窗而非静默回默认；⑥删 json 重建正常。
5. 每处修复在最终报告中标注验证方式与结果（断言名/命令输出）。

## 五、交付物

最终输出三份清单：**问题清单**（定位 file:line / 成因 / 严重程度，含"仅记录不动"项）、**修复措施对照表**、**验证结果**（自测输出摘要 + NX 实测清单）。不更新 docs/（你的偏好：文档只在要求时更新）。